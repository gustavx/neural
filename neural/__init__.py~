
# Dentro del modulo usar neural.num para tener la alternativa de poder
# reemplazar numpy por otro modulo (e.g. uno propio en python puro, uno
# que use GPU, etc).
import numpy as num
num.set_printoptions( precision=3, suppress=True, linewidth=160)

from unit import unit

